export enum ResultCodes {
  Success = 0,
  Error = 1,
}

export enum DoubleRangeNumber {
  percentage = 100,
  minDistance = 1,
}

export enum Numbers {
  Zero = 0,
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
  Five = 5,
  Six = 6,
  Seven = 7,
  Eight = 8,
  Nine = 9,
  Ten = 10,
}

export enum ResultMethodeSort {
  Default = 0,
  Descending = -1,
  Ascending = 1,
}

export enum RouteLink {
  login = '/login',
  registration = '/registration',
  restorePassword = '/restorePassword',
  pageCard = '/pageCard',
  card = '/card/:id',
  profile = '/profile',
  newPassword = '/newPassword/:token',
  authMe = '/authMe',
}

export enum Learn {
  initialValue = 0,
  acc = 0,
  numberSmart = 6,
  validValue = 1,
}

export enum Data {
  start = 0,
  finish = 10,
}
